import React from "react";
import { Layout } from "antd";

const { Footer } = Layout;

const footer = () => (
  <Footer style={{ textAlign: "center" }}>
    Created use @NewsAPIorg and antd
  </Footer>
);

export default footer;
